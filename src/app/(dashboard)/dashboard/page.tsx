import { Base } from '../_components/base/base'

const DashboardPage = () => {
  const title = [
    {
      label: 'Dashboard',
      href: '/dashboard',
    },
  ]

  return <Base title={title}>opa</Base>
}

export default DashboardPage
